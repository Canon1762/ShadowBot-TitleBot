# ShadowBot-TitleBot小白搭建教程

## 这份教程解决什么问题

只做一件事：

```text
从 Chrome/Gemini 当前回复读取“商品标题”
在 WPS 当前绿色行中，把连续占位符“，，”之间替换成该标题
每成功一行，下一行加 4
```

不调用任何 AI API，不使用 OCR，不使用屏幕坐标，不修改下面三行公式。

> 重要：你装的是 **影刀 6.3.22 社区版（64 位）**。  
> 6.3.x 的界面和 6.2.x 不一样：新版是“可视化流程 + 左侧指令区 + 积木输出变量”，旧版 6.2.23 教程里的“组件库、变量面板、Python 组件输入输出”不能照抄。  
> 本文按 6.3.22 的实际界面写。推荐用“一个 Python 代码段跑完全程”，这是 6.3.22 里最省事、最少踩坑的做法。

---

## 一、开始前先做 3 件事

### 1. 确认影刀版本

1. 打开影刀 RPA。
2. 看左上角或“关于”里的版本号，应为 **6.3.22**。
3. 社区版 64 位即可，不需要企业版。

### 2. 装好 Chrome 自动化插件

6.3.22 的网页自动化依赖“自动化插件”：

1. 打开影刀 6.3.22。
2. 在顶部工具栏找到 **自动化插件**（有的界面在“插件”里）。
3. 选择 **安装插件** 或 **修复插件**，按提示安装 Chrome 插件。
4. 安装完成后，**完全关闭 Chrome，再重新打开**。
5. 打开 Gemini 页面，确认你已经登录。

如果你不想用 Chrome，也可以用影刀浏览器（后面把 `BROWSER_MODE` 改成 `"cef"`），但第一次建议先用 Chrome 把流程跑通。

### 3. 打开两个窗口，准备一条测试数据

保持 Chrome 的 Gemini 页面打开，保持 WPS 表格目标文件打开。

第一次调试时让两个窗口并排，方便观察。正式运行时影刀会自己连接，不依赖窗口摆放位置。

在 WPS 第一个绿色行中放入测试数据。例如目标是 `B2`，就放在 `B2`：

```text
自粘墙纸 PVC 可移除，易安装不留残胶，，客厅卧室厨房家具翻新装饰墙贴。
```

在 Gemini 当前回复中准备：

```text
商品标题：极简线描野生飞燕草与洋甘菊，清新舒缓莫兰迪自然氛围
```

第一次只测试这一条，不要直接运行全部数据。

---

## 二、新建 6.3.22 可视化流程

### 第 1 步：新建应用

1. 打开影刀 6.3.22。
2. 进入 **我的应用**。
3. 点 **新建应用**。
4. 应用名称随便填，例如 `Gemini标题写入WPS_测试`。
5. 创建后点 **打开应用**，进入流程设计器。

### 第 2 步：确认你看到的是“主流程”

打开应用后，左侧/中间的流程树里应该能看到 **主流程**。

如果只看到“流程”分组、没有主流程：

1. 在“流程”上点右键。
2. 选择 **新建流程**。
3. 选择 **可视化流程**。
4. 名称填 `主流程`。

### 第 3 步：拖入一个 Python 代码段积木

6.3.22 的流程设计器是“从左侧拖入指令，像搭积木一样搭建自动化流程”。

1. 在左侧找到 **搜索指令** 输入框。
2. 搜索 `代码段` 或 `Python`。
3. 找到说明为 **插入代码段(Python)** 的积木。
4. 把它拖到 **主流程** 里，或者点积木右侧的“添加”。

### 第 4 步：粘贴完整代码

1. 点中刚拖进来的 **插入代码段(Python)** 积木。
2. 右侧会出现代码输入框。
3. 打开工作区里的 [yingdao_6.3.22_all_in_one.py](/C:/Users/Administrator/Documents/Titlecopy/yingdao_6.3.22_all_in_one.py)。
4. 从第一行到最后一行**整段复制**，粘贴到影刀的代码输入框。
5. 按 `Ctrl+S` 或点顶部的 **保存**。

这段代码已经包含：

```text
连接已打开的 Chrome/Gemini 页面
读取“商品标题：”后面的标题
清洗 Gemini 返回的 Markdown/多行文本
连接当前激活的 WPS 工作簿
读取绿色行的原文本
把“，，”之间替换成标题
写回 WPS
立刻回读校验
失败就报错停止，不会继续写下一行
```

---

## 三、只改这 10 个设置

打开 `yingdao_6.3.22_all_in_one.py` 顶部，找到 **只改这里** 这一段。第一次测试按下面填：

| 设置                         | 第一次测试                 | 正式批量                   | 说明                                    |
| -------------------------- | --------------------- | ---------------------- | ------------------------------------- |
| `TARGET_COL`               | `"B"`                 | 例如 `"B"`               | 目标列                                   |
| `START_ROW`                | `2`                   | 实际第一个绿色行，例如 `86`       | 第一个绿色行                                |
| `ROW_STEP`                 | `4`                   | `4`                    | 每成功一行，下一行加 4                          |
| `MAX_ITEMS`                | `1`                   | 实际条数                   | 先测试 1 条                               |
| `SHEET_NAME`               | `""`                  | 留空或填实际工作表名             | 留空表示当前激活工作表                           |
| `GEMINI_URL_KEYWORD`       | `"gemini.google.com"` | 同左                     | 用网址匹配 Gemini 页面                       |
| `BROWSER_MODE`             | `"chrome"`            | `"chrome"`             | Chrome；Edge 用 `"edge"`；影刀浏览器用 `"cef"` |
| `READ_MODE`                | `"single"`            | `"single"` 或 `"batch"` | 先单条；能一次读多条时再改 batch                   |
| `WAIT_BEFORE_READ_SECONDS` | `0`                   | `0` 或 `1`              | Gemini 还在生成时填 1                       |
| `SAVE_AFTER_RUN`           | `False`               | `False` 或 `True`       | `True` 表示跑完后自动保存 WPS；第一次测试保持 `False`  |

如果第一个绿色行是第 86 行：

```python
START_ROW = 86
```

后面的行仍然是 `86、90、94……`，不需要改 `ROW_STEP`。

---

## 四、先跑 1 行

### 1. 运行前检查

运行前确认：

```text
Chrome 的 Gemini 页面已经打开，并已登录
WPS 目标工作簿已经打开
Chrome 和 WPS 不要最小化；如果 WPS 写入失败，先点一下 WPS 窗口再运行
WPS 里只有这一个工作簿，避免影刀找不到“当前激活的 Excel”
WPS 和影刀用同样的权限运行（都普通，或都管理员）
代码顶部：START_ROW = 2，MAX_ITEMS = 1
B2 里已经有带“，，”的测试文本
Gemini 当前回复里有“商品标题：……”
```

### 2. 点运行

1. 在影刀顶部点 **运行**。
2. 等待流程结束。
3. 看底部的 **运行日志**。

正确日志应类似：

```text
[第2行] 成功：自粘墙纸 PVC 可移除，易安装不留残胶，极简线描野生飞燕草与洋甘菊，清新舒缓莫兰迪自然氛围，客厅卧室厨房家具翻新装饰墙贴。
完成：成功 1 行。
```

### 3. 检查 WPS

回到 WPS，看 `B2`：

```text
自粘墙纸 PVC 可移除，易安装不留残胶，极简线描野生飞燕草与洋甘菊，清新舒缓莫兰迪自然氛围，客厅卧室厨房家具翻新装饰墙贴。
```

注意：原来的三个中文逗号没有被破坏，只是 `，，` 之间被插入了标题。

---

## 五、单条成功后再测 3 条

把代码顶部改成：

```python
MAX_ITEMS = 3
```

并确认目标行是：

```text
2、6、10
```

或者如果第一个绿色行是 86：

```text
86、90、94
```

三行都要提前放好带 `，，` 的测试文本。三条成功后，再把 `MAX_ITEMS` 改成正式数量。

### 如果 Gemini 一次能显示多条标题

把代码顶部改成：

```python
READ_MODE = "batch"
MAX_ITEMS = 实际条数
```

代码会先读取 Gemini 最后一条回复里所有的 `商品标题：`，然后按顺序写入 `START_ROW、START_ROW+4、START_ROW+8……`。

使用 batch 前，必须等 Gemini 把这一批全部生成完，不要再让它继续输出。

---

## 六、6.2.23 旧组件 → 6.3.22 新积木对照

如果你之前看过 6.2.23 的教程，下面这张表可以直接帮你换名字。6.3.22 的积木在左侧“搜索指令”里按说明文字搜索。

| 6.2.23 旧写法        | 6.3.22 新积木（搜索关键词）         | 6.3.22 内部名称                                             |
| ----------------- | ------------------------- | ------------------------------------------------------- |
| 获取已打开的网页          | 获取网址或标题匹配已打开的网页           | `web.get`                                               |
| 执行 JavaScript     | 在指定的网页中执行一段 Javascript 脚本 | `web.browser.execute_javascript`                        |
| 获取已打开的 Excel      | 获取当前激活的 Excel 对象          | `excel.get_active_workbook`                             |
| 打开/绑定 WPS 文件      | 打开已有 Excel 文件，或新建 Excel   | `excel.launch`                                          |
| 读取 Excel 单元格      | 从 Excel 文件中读取内容           | `excel.read_data_from_workbook`                         |
| 写入 Excel 单元格      | 在 Excel 工作表中写入内容          | `excel.write_data_to_workbook`                          |
| Python 组件（带输入/输出） | 插入代码段(Python)             | `programing.snippet`                                    |
| 激活窗口              | 激活指定窗口使其在前台运行             | `win32.window.activate`                                 |
| 指定次数循环            | 对一组指令进行指定次数的循环操作          | `workflow.for`                                          |
| 循环结束              | 表示一个循环区域的结尾               | `workflow.endloop`                                      |
| 条件判断              | 条件判断开始标记                  | `workflow.if`                                           |
| 条件判断结束            | 条件判断结束标记                  | `workflow.endif`                                        |
| 尝试执行/异常捕获         | 尝试执行开始标记、尝试失败时执行、尝试执行结束标记 | `programing.try`、`programing.catch`、`programing.endtry` |
| 写入日志              | 将变量或者文本打印输出               | `programing.log`                                        |
| 停止流程              | 停止整个应用的运行                 | `process.exit`                                          |
| 抛出异常              | 抛出异常                      | `programing.raise`                                      |

### 6.3.22 最重要的变化

1. **新版是“可视化流程”**：左侧指令区拖到主流程里，不再叫“组件库”。
2. **变量跟积木输出绑定**：大多数积木的“输出”字段填一个变量名，就自动创建/引用变量；右侧还有“全局变量”面板。
3. **Python 代码段不是旧版 Python 组件**：代码段里的局部变量不会自动变成流程变量。所以本文推荐“一个代码段跑完全程”，不折腾积木之间的变量传递。
4. **运行日志在底部**：出问题先看 `运行日志`，再看 `流程错误`。
5. **自动化插件必须装**：6.3.22 网页自动化依赖“自动化插件”，Chrome 装完要重启。

---

## 七、纯积木版怎么搭（可选，不推荐新手先用）

如果你坚持不用一个代码段跑完全程，6.3.22 的积木顺序是：

```text
主流程
├─ 获取当前激活的 Excel 对象（或：打开已有 Excel 文件，驱动选 WPS）
├─ 获取网址或标题匹配已打开的网页（浏览器选 Chrome，网址填 gemini.google.com）
├─ 对一组指令进行指定次数的循环操作
│   start = START_ROW
│   stop  = START_ROW + (MAX_ITEMS - 1) * ROW_STEP
│   step  = ROW_STEP
│   输出变量名 = current_row
│  ├─ 在指定的网页中执行一段 Javascript 脚本
│  │   输出变量名 = raw_title
│  ├─ 从 Excel 文件中读取内容
│  │   读取方式 = 单元格
│  │   行号 = current_row，列名 = TARGET_COL
│  │   输出变量名 = old_text
│  ├─ 插入代码段(Python)
│  │   读取 glv['raw_title']、glv['old_text']、glv['current_row']
│  │   写入 glv['result_text']、glv['ok']、glv['error_message']
│  ├─ 条件判断开始标记（ok 为 False 时 → 打印错误 + 停止整个应用）
│  ├─ 在 Excel 工作表中写入内容
│  │   写入区域 = 区域，写入方式 = 覆盖
│  │   起始行号 = current_row，起始列名 = TARGET_COL
│  │   写入内容 = result_text
│  ├─ 从 Excel 文件中读取内容（输出变量名 = verify_text）
│  ├─ 条件判断开始标记（verify_text != result_text 时 → 报错停止）
│  ├─ 将变量或者文本打印输出（成功日志）
│  └─ 表示一个循环区域的结尾
└─ 完成
```

纯积木版开始前，先在右侧 **全局变量** 面板点 **新增变量**，至少建好：

```text
current_row
raw_title
old_text
clean_title
result_text
ok
error_message
verify_text
```

然后把每个积木的“输出变量名”填成上面这些名字。否则 Python 代码段里的 `glv['raw_title']` 会读不到。

这一段只适合你已经理解 6.3.22 的积木变量机制之后再做。第一次请先用前面的“一个代码段”版本把业务跑通。

---

## 八、常见报错怎么判断

| 日志或现象                                   | 原因                          | 处理                                                                         |
| --------------------------------------- | --------------------------- | -------------------------------------------------------------------------- |
| `未找到匹配的网页`                              | Chrome 插件没装好，或 Gemini 页面没打开 | 影刀顶部“自动化插件”里安装/修复 Chrome 插件，重启 Chrome；确认 Gemini 页面网址包含 `gemini.google.com` |
| `NameError: name 'xbot' is not defined` | 个别版本的代码段命名空间限制              | 先在代码段第一行加 `import xbot`；如果还不行，改用第 7 节的积木版绑定网页和 Excel，代码段只保留字符串清洗和替换        |
| `不能获取 office 或者 wps 的当前激活 excel`        | WPS 没打开，或同时开了多个 Excel/WPS   | 只保留目标 WPS 工作簿；关闭其它 Excel/WPS 窗口；必要时在积木里用“打开已有 Excel 文件”显式绑定 WPS            |
| `请以管理员身份打开 Excel` / `请以非管理员身份打开 Excel`  | WPS 和影刀的权限不一致               | 让 WPS 和影刀都普通运行，或都管理员运行                                                     |
| `WPS 当前单元格为空`                           | 行号、列号、工作表选错                 | 检查 `TARGET_COL`、`START_ROW`、`SHEET_NAME`                                   |
| `没有找到连续的中文逗号占位符“，，”`                    | 当前单元格不是绿色目标行，或没有 `，，`       | 停止；检查该行是否真的是绿色目标行                                                          |
| `Gemini 没有读取到“商品标题：”内容`                 | Gemini 还在生成、当前回复不对、或页面结构变了  | 等生成完；确认当前回复里有 `商品标题：`；把 `WAIT_BEFORE_READ_SECONDS` 改成 1 再试                 |
| `回读校验不通过`                               | 写入的不是值/文本，或写到了错误单元格         | 检查 WPS 单元格是否合并、是否是公式；检查列和行                                                 |
| 只改了第 2 行，下面没变                           | 下面三行公式没有刷新                  | 不要写入下面三行；检查原始公式是否还在                                                        |
| 运行日志里中文显示乱码                             | 控制台编码问题                     | 不影响 WPS 写入；以 WPS 里的实际结果为准                                                  |

---

## 九、最省事的搭建顺序

如果你完全不想一次搭很多积木，就按这个顺序做：

1. 先只做“一个代码段”，粘贴完整代码。
2. `MAX_ITEMS = 1`，只测 `B2`。
3. 看运行日志，确认标题清洗正确。
4. 看 WPS `B2`，确认 `，，` 之间被正确替换。
5. 改成 `MAX_ITEMS = 3`，测 `2、6、10`。
6. 三条成功后，再改成正式数量。
7. 确认整批没问题后，如果 Gemini 一次能显示多条，再把 `READ_MODE` 改成 `"batch"`。

这样每一步都能定位问题，不会因为一个积木填错而把整张表弄乱。

---

## 十、一句话版本

```text
影刀 6.3.22 → 我的应用 → 新建应用 → 主流程
→ 搜索“插入代码段(Python)” → 拖入 → 粘贴 yingdao_6.3.22_all_in_one.py
→ START_ROW 填第一个绿色行，MAX_ITEMS 填 1
→ 运行 → 看运行日志 → 看 WPS
→ 成功后再把 MAX_ITEMS 改成正式数量
```
